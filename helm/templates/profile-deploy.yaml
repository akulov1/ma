apiVersion: apps/v1
kind: Deployment
metadata:
  name: profile-service
  namespace: {{ .Values.namespace }}
  labels:
    app: profile-service
    {{- include "end-platform.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount.profile }}
  selector:
    matchLabels:
      app: profile-service
  template:
    metadata:
      labels:
        app: profile-service
        {{- include "end-platform.labels" . | nindent 8 }}
    spec:
      containers:
        - name: profile-service
          image: "{{ .Values.images.profile.repository }}:{{ .Values.images.profile.tag }}"
          imagePullPolicy: {{ .Values.images.profile.pullPolicy }}
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef: { name: app-config }
            - configMapRef: { name: ui-config }
          env:
            - name: DB_USER
              valueFrom: { secretKeyRef: { name: db-secret, key: DB_USER } }
            - name: DB_PASSWORD
              valueFrom: { secretKeyRef: { name: db-secret, key: DB_PASSWORD } }
            - name: DB_NAME
              valueFrom: { secretKeyRef: { name: db-secret, key: DB_NAME } }
          livenessProbe:
            httpGet: { path: /health/live, port: 8000 }
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet: { path: /health/ready, port: 8000 }
            initialDelaySeconds: 30
            periodSeconds: 10
