apiVersion: v1
kind: Service
metadata:
  name: profile-service
  namespace: {{ .Values.namespace }}
  labels:
    app: profile-service
    {{- include "end-platform.labels" . | nindent 4 }}
spec:
  type: NodePort
  selector:
    app: profile-service
  ports:
    - name: http
      port: 80
      targetPort: 8000
      nodePort: {{ .Values.service.nodePorts.profile }}
