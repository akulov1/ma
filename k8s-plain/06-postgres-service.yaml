apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: user-platform-exam
  labels:
    app.kubernetes.io/name: postgres
    app.kubernetes.io/part-of: user-platform
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: postgres
  ports:
    - name: pg
      port: 5432
      targetPort: 5432
